import React from "react";
import classes from "./DateRangeSelector.module.css";

const DateRangeSelector: React.FC = () => {
  return <div className={classes.container}>DateSelector</div>;
};

export default DateRangeSelector;
